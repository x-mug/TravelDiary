<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/img_gallery"
        android:layout_width="match_parent"
        android:layout_height="@dimen/height_gallery"
        android:background="@color/gallery_background2"
        android:scaleType="centerCrop"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageButton
        android:id="@+id/imgBtn_addImage"
        android:layout_width="@dimen/add_gallery_button"
        android:layout_height="@dimen/add_gallery_button"
        android:background="@mipmap/ic_add_image"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="@+id/img_gallery"
        app:layout_constraintStart_toStartOf="@+id/img_gallery"
        app:layout_constraintTop_toTopOf="@+id/img_gallery" />

</androidx.constraintlayout.widget.ConstraintLayout>